# ⚠️  This file is kept for backward compatibility only.
# Dependencies are now managed via pyproject.toml.
#
# Install with:
#   pip install -e .              # runtime dependencies
#   pip install -e .[dev]         # + mypy, ruff, pre-commit, pytest
#   pip install -e .[advanced]    # + XGBoost, Optuna, MLflow
#
# The canonical list of dependencies lives in pyproject.toml [project.dependencies].

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
scikit-learn>=1.4.0
pandas>=2.2.0
numpy>=1.26.0
joblib>=1.4.0
gradio>=4.31.0
httpx>=0.27.0
prometheus-client>=0.20.0
prometheus-fastapi-instrumentator>=7.0.0
structlog>=24.1.0
