# Advanced training dependencies — install on top of the base package.
# Preferred: pip install -e .[advanced]
# Legacy:    pip install -r requirements.txt -r requirements-advanced.txt
xgboost>=2.0.0
optuna>=3.6.0
mlflow>=2.13.0
